server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: main #le indico rama x defecto
          uri: https://github.com/marcelawojtasik/BootcampJAVA.ConfigServer.git
          search-paths: config-data
  application:
    name: ConfigServer

